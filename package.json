{
  "name": "my-landing",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "spago bundle-app --main Main --to src/index.js && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
